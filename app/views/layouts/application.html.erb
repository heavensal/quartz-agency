<!DOCTYPE html>
<html>
  <head>
    <title>Quartz Agency</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
  </head>

  <body class="bg-gradient-to-l from-sky-100 via-sky-50 to-sky-100">
    <div data-controller="scroll-progress" class="h-2 bg-indigo-400 fixed bottom-0 z-30"></div>

    <% unless controller_name == 'pages' && action_name == 'home' %>
      <%= render 'layouts/navbar' %>  <!-- Remplace par le chemin vers ta navbar -->
    <% end %>

    <main class="font-poppins">
      <%= yield %>
    </main>
  </body>
</html>
